# ExamGPT — Screen OCR → ChatGPT

A Windows desktop tool that lets you select a screen region, OCR it, and send the text to ChatGPT.

## Run
```powershell
# From repo root
python -m pip install -r requirements.txt
python start.py
